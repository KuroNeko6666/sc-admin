export const environment = {
  api: {
    host: "http://localhost:8080/api/admin",
    path: {
      auth: "/auth",
      device: "/device",
      deviceData: "/device/data",
      dashboard: "/dashboard/",
      admin: "/admin",
      user: "/user",
      order: "/order",
    }
  }
};
